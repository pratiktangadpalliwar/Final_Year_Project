apiVersion: v1
kind: Service
metadata:
  name: fl-server
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP
  selector: { app: fl-server }
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
